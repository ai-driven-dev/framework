# AIDD Installation Guide

> Installation guide for **{{TOOL_NAME}}** (AIDD Framework v{{VERSION}}).

## Step 1: Copy the framework files

Copy the AIDD dist into your project root:

```bash
cp -r path/to/aidd/framework/dist/{{TOOL_KEY}}/* your-project/
```

Your project should look like this:

```text
{{FILE_TREE}}
```

## Step 2: Configure your project

1. Open the `aidd_docs/README.md` file and follow the onboarding instructions.
2. Fill in the memory templates to teach the AI about your project.

For the full documentation, see [aidd_docs/README.md](../../README.md).
