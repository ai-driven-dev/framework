---
name: audit
description: Full read-only audit of the AIDD CLI across all seven quality pillars
argument-hint: N/A
---

# Codebase Audit: AIDD CLI (full)

Structurally healthy hexagonal CLI. **No confirmed critical** — every critical from the 2026-06-11 audit was verified remediated in commit `305fe7e`. Remaining work is supply-chain hardening on untrusted catalogs, sequential-fetch latency, the project's own clean-code rules (empty catches, validator casts, ≤20-line methods), and a coverage gap on the `global/` orchestrators.

- **Date**: 2026-06-26
- **Scope**: `src/` — 258 files, ~23.6k LOC, all seven pillars (UI = CLI terminal output; web-UI dimensions N/A)
- **Health**: good — no confirmed critical findings; warnings are isolated and addressable
- **Findings**: 0 critical, 19 warning, 8 minor

Health: `good` = no critical · `fair` = critical but isolated · `poor` = systemic.
Severity: 🔴 broken/exploitable now · 🟡 real debt/risk · 🟢 nit. Effort: `S` <1h · `M` <1d · `L` >1d.

## Reconciliation with the 2026-06-11 audit

The prior full audit (this file's git history, last rev under commit `305fe7e "fix(cli): remediate June 2026 6-pillar audit"`) reported 7 criticals. **All 7 verified remediated against current code:**

| Prior critical | Status | Evidence (current code) |
| --- | --- | --- |
| fast-uri CVE GHSA-q3j6 (prod) | ✅ fixed | `package.json` pnpm.overrides `fast-uri: ">=3.1.2"` |
| fast-uri CVE GHSA-v39h (prod) | ✅ fixed | same override |
| dep-policy breach (6 vs 2 runtime deps) | ✅ resolved | policy amended; 6 deps all actively imported |
| cli.ts cold-cache network on hot path (~600ms) | ✅ fixed | `cli.ts:69` preAction = `printFromCacheOnly()` (offline); `cli.ts:84` postAction = `refresh()` piggybacked on online commands — matches the `3-cli-lifecycle` rule |
| `sync-status-use-case` 0% | ✅ fixed | 100% lines |
| `marketplace-cache-adapter` 14% | ✅ fixed | 92.95% lines |
| `install-{commands,skills,…}` 10-13% | ✅ fixed | 100% lines |

The prior's 🟡 security items were also fixed: pnpm option injection (`plugin-fetcher-adapter.ts:127` now `["add","--prefix",cacheDir,"--",spec]`), prototype pollution (`file-adapter.ts:218` `PROTOTYPE_POLLUTION_KEYS` guard), symlink disclosure (`file-adapter.ts:91,156` `isSymbolicLink()` skip). Severities below for icacls / sha-not-verified / http-scheme are **aligned to the prior team's 🟢 triage** — nothing in those areas changed.

## Findings

One row per issue, concrete `file:line`, sorted severity-first across all pillars. Read-only: this audit reports, it never edits.

| Sev | Category     | Location | Issue | Suggested fix | Effort |
| --- | ------------ | -------- | ----- | ------------- | ------ |
| 🟡 | security | `src/domain/formats/copilot-marketplace-catalog.ts:58` | Untrusted catalog `obj.source` concatenated as `${pluginRoot}/${obj.source}` with no `..` guard — a compromised/updated trusted marketplace catalog can point a local source outside the plugin root (not flagged in prior audit) | Reject `..`/absolute; `resolve` then assert result stays under `pluginRoot` | M |
| 🟡 | dependencies | transitive via vitest/tsup/stryker/commitlint | esbuild GHSA-67mh-4wv8-2f99 + vite/js-yaml/@babel advisories — DEV-only, absent from shipped `dist`, no runtime exposure | `pnpm update` to advised targets (vite 7.3.6, js-yaml 4.2.0, @babel/core 7.29.7) | S |
| 🟡 | performance | `src/application/use-cases/marketplace/marketplace-list-use-case.ts:53` | Catalog fetched sequentially per marketplace in an awaited loop | `Promise.all(marketplaces.map(...))` then flatten | M |
| 🟡 | performance | `src/application/use-cases/marketplace/marketplace-list-use-case.ts:56` | `forceRefresh: true` on every list call defeats the cache adapter | Only force on the explicit `marketplace refresh` command | S |
| 🟡 | performance | `src/application/use-cases/plugin/plugin-install-from-marketplace-use-case.ts:101` | Sequential awaited catalog resolution per marketplace in a loop | Parallelize match-lookups with `Promise.all` | M |
| 🟡 | performance | `src/application/use-cases/restore/restore-all-plugins-use-case.ts:65` | Plugin fetches (git clone) run sequentially per plugin | Parallelize fetches; keep manifest writes serialized | M |
| 🟡 | code-quality | `src/application/use-cases/doctor/doctor-use-case.ts:59` | Empty `catch {}` swallows manifest parse error — violates project rule "no silent errors" | Translate to a typed domain error before rethrow | S |
| 🟡 | code-quality | `src/application/use-cases/framework/framework-build-use-case.ts:59` | Empty `catch {}` loses file-read failure context | Wrap original error message into the thrown domain error | S |
| 🟡 | code-quality | `src/application/use-cases/marketplace/marketplace-sync-settings-use-case.ts:43` | `.catch(() => null)` masks manifest-load failure as "no manifest" — use-cases must throw, not swallow | Remove catch; let it propagate to `errorHandler` | M |
| 🟡 | code-quality | `src/application/use-cases/framework/framework-build-use-case.ts:88` & `strategies/marketplace-build-strategy.ts:131` | `as unknown as SourceMarketplace` double-cast masks an incomplete validator (no plugin-shape checks) | Have the validator return a typed result; drop the casts | M |
| 🟡 | code-quality | `src/infrastructure/deps.ts:356-740` | `createDeps` is ~384 lines instantiating 65+ use-cases inline — readability/SRP risk (wiring only, no business logic) | Split into grouped private factories (`createFrameworkDeps`, `createPluginDeps`, …) | L |
| 🟡 | code-quality | `src/application/use-cases/sync/sync-file-propagation-use-case.ts:325-374` | `propagateOneAdded` ~49 lines / 4-level nesting — exceeds the ≤20-line method rule | Extract `validateAddedFile` + `transformAndWrite` | M |
| 🟡 | code-quality | `src/application/use-cases/sync/sync-file-propagation-use-case.ts:175-217` | `runAllPropagations` threads 6+ params through 5 private methods | Introduce a `PropagationContext` passed once | M |
| 🟡 | code-quality | `src/domain/models/manifest.ts:91-155` | Five `migrateVNtoVN+1` functions share identical structure + repeated `as Record<...>` assertions | Drive from a `MigrationStep[]` loop | L |
| 🟡 | code-quality | `src/application/use-cases/marketplace/marketplace-sync-settings-use-case.ts:87-100` | `activateTool` nests 3 levels with implicit early returns — control flow hard to trace | Flatten with guard clauses; extract the activation loop | M |
| 🟡 | code-quality | `src/application/use-cases/install/install-ai-tool-use-case.ts:62` | `getInstalledToolIds() as AiToolId[]` assumes all installed IDs are AI tools, unchecked | Filter by `isAiTool(getToolConfig(id))` before cast | S |
| 🟡 | architecture | `src/application/commands/marketplace.ts:65` | Command mutates global `process.env.AIDD_TOKEN` instead of threading the token as a use-case param | Pass token explicitly (see `plugin.ts:153` pattern) | M |
| 🟡 | architecture | `src/application/commands/setup.ts:148` | `SetupUseCase` (12-arg orchestrator) instantiated in the command rather than supplied by `createDeps` | Expose `setupUseCase` on the `Deps` graph | M |
| 🟡 | tests | `src/application/use-cases/global/` (doctor-all 13%, sync-plugins 13.8%, restore-all 14.8%, status-all 18.3%, update-all 29.6%, sync-all 32.7%) | The `global/` delegating orchestrators back top-level commands yet are largely untested despite 87.5% overall line coverage | Add an integration test per `global/*-all` use-case | M |
| 🟢 | security | `src/infrastructure/auth/auth-storage.ts:44` | `execSync(\`icacls "${path}"…\`)` builds a shell string from a path (Windows-only, self-targeted) | `execFileSync("icacls",[path,…])` — no shell (prior triaged 🟢) | S |
| 🟢 | security | `src/domain/models/plugin-source.ts:~63` | `sha` field parsed but never verified after clone (clone pins a mutable ref) | Verify `HEAD == sha` post-clone, or drop the field (prior triaged 🟢) | M |
| 🟢 | security | `src/domain/models/plugin-source.ts:~170` | `parsePluginSourceShorthand` accepts `http://` source URLs (defense-in-depth; simple-git pinned) | Require `https`/`git@`; allow `http` only for localhost (prior triaged 🟢) | S |
| 🟢 | dependencies | `package.json` | 14 outdated deps incl. majors: commander 12→15, typescript 5→6, vitest 3→4, lefthook 1→2 | Schedule a batched major-bump pass with a full test run | M |
| 🟢 | tests | `src/application/display/{setup,status,doctor}-display.ts:1` | Display modules at 0% coverage (console-output only) | Add thin output-formatting assertions, or document the exclusion | S |
| 🟢 | code-quality | `src/application/use-cases/framework/strategies/tool-contracts.ts:664,794` | `Object.keys(x).length === 0` emptiness check duplicated | Extract `isEmpty(obj)` helper | S |
| 🟢 | code-quality | `src/application/use-cases/framework/strategies/tool-contracts.ts:78,86,469` | Unused `_plugin` param applied inconsistently across transform fns | Apply the `_`-prefix convention uniformly | S |
| 🟢 | code-quality | `src/application/use-cases/framework/strategies/tool-contracts.ts:104,285` | `[...args] as const` tuple-spread into `resolveVersion` obscures argument intent | Pass a named-parameter object | S |

## Top actions (ranked by impact)

1. **Harden the untrusted-catalog path** — guard `copilot-marketplace-catalog.ts:58` against `..` traversal (highest residual attack surface, not covered by the June remediation). Hand off to `refactor` (security axis).
2. **Clear the dev-dependency CVEs** — `pnpm update` to advised targets removes all current `pnpm audit` noise; cheap and dev-only. Hand off to `refactor`.
3. **Cover the `global/` orchestrators** — doctor-all/sync-plugins/restore-all/status-all sit at 13–18% yet back top-level commands. Hand off to `test`.
4. **Parallelize sequential marketplace/plugin fetches + fix `forceRefresh`** — visible latency win on `marketplace list` / `plugin install` / `restore`. Hand off to `refactor` (performance axis).
5. **Enforce the project's own clean-code rules** — empty `catch {}` / `.catch(()=>null)` swallows (violate "no silent errors"), `as unknown as` validator casts, ≤20-line method overruns in `sync-file-propagation` + `deps.ts`. Hand off to `refactor` (cleanup axis).

## Coverage

- **Scanned**: `security` (manual read of auth/adapters/http/catalog + secret grep — none found; all `child_process` sinks reviewed; prior security fixes re-verified), `code-quality` (largest-file read-through + empty-catch/cast grep; jscpd ran but its 52% figure is self-match noise — each reported clone is a file vs itself — so excluded from ranking), `architecture` (layering/purity/cycle grep — domain clean, see below), `dependencies` (`pnpm audit` + `pnpm outdated` + `knip --production` = no unused production code), `performance` (sync-I/O + sequential-await grep + cache review; prior hot-path bug re-verified fixed), `tests` (coverage-summary.json: 87.5% lines / 87% branches / 90% funcs; low-coverage files enumerated), `ui` (CLI: output via `application/output.ts` + `error-handler.ts` + `display/`; web-UI dimensions — responsive/a11y/design-system — N/A for a headless CLI; the analogue, display-layer coverage, logged as a tests finding).
- **Architecture clean dimensions** (verified, no findings): domain imports no `application`/`infrastructure`; `domain/formats` + `domain/models` contain no `node:fs`/`child_process`/`fetch`/`process.*`; no use-case import cycles; adapters implement ports via `implements`, none `new` other adapters; `deps.ts` body is sequential wiring, not business logic.
- **Skipped**: none — all seven pillars examined. (`gosec`/web-a11y tooling not applicable to a TS CLI; covered by manual review instead.)
