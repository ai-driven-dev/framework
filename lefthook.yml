pre-commit:
  output: true
  commands:
    biome:
      run: pnpm lint
    typecheck:
      run: pnpm typecheck

pre-push:
  commands:
    knip:
      run: pnpm knip:production
    test:
      run: pnpm test

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

