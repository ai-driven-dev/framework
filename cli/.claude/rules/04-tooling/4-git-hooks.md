---
paths:
  - "lefthook.yml"
---

# Git Hooks (lefthook)

- `pre-commit` — biome + typecheck
- `pre-push` — knip + tests
- `commit-msg` — Conventional Commits (commitlint)
- Never bypass with `--no-verify`
