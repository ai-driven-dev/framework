---
description: Apply when writing or keeping a comment; a comment says what the code cannot, and names no external reference.
paths:
  - "src/**/*.ts"
  - "tests/**/*.ts"
  - "scripts/**"
---

# Comments

- Comment only what code and names cannot say.
- Worth one: a constraint types miss, a measured host quirk, a refused simpler shape.
- Delete a comment that restates code or narrates history.
- Name no ticket, PR, commit, date, review or URL.
- Keep the fact and its consequence.
- One or two lines; a paragraph belongs in `aidd_docs/memory/`.
- `tests/architecture/comments.arch.test.ts` refuses external references and ratchets volume down.
