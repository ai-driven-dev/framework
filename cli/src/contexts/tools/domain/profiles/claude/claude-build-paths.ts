/** Claude build output path constants, deliberately distinct from the source-side constants
 * even where the literal values coincide: a change to either side must not collapse them. */

export const OUTPUT_CLAUDE_MANIFEST_RELATIVE = ".claude-plugin/plugin.json";

export const OUTPUT_CLAUDE_MARKETPLACE_RELATIVE = ".claude-plugin/marketplace.json";
