<!-- Onboard screen shapes. 03-present fills the one 02-assess chose. Match the mockup gallery in the plan folder. -->

<!--
State-block glyphs (single home): ✅ done · ⚠️ present, not wired (carries a fix) · ❌ required, missing. Unused tools omitted, never crossed.
Keys render inline as bracketed tokens ([1] act · [OK] walk · [m] map · [?] detail); routing is in run/replies.md, the tier clause (in [?] only) in run/tiers.md.
Framing line shows on the first report of the session only, then drops.
-->

## Entry screen — greenfield · existing · idle · drift

```txt
<banner.txt>

👋  <framing, or "Welcome back.">

Your AIDD setup:
  AI tools   <detected, each ✅/⚠️>
  Plugins    <installed>   ✅
  Memory     <aidd_docs · N synced ✅ | ❌ not set up yet>

<state sentence, or a ⚠️ warning line, see Warning line>

👉  <top action>   [1]
    <other keyed options>   ·   flow [m]
```

Idle adds the flow and `[1] walk · [2] SDLC`, per `flow.md`.

## Foundations step

```txt
Foundations — step <n> of <total>

  <deliverable>     <what, plain>
                    (<why>)

👉  Type [1] to start.   skip [s] · what's this [?]
```

`<total>` and the step order come from `flow.md` (2 for existing, 3 for greenfield).

## Where you are — mid-work

```txt
🔍  <state sentence>

  Flow:  <the 8-step path, current stage [bracketed], done stages ✅>

👉  Next: <step>   [1]
    <other keyed options>
```

## Warning line

A `⚠️` never stands alone — its plain cause, then a keyed fix:

```txt
⚠️  <what is wrong, plain>
   → Type [1] to <fix>.
```

## [?] detail

Expands the decision: each step's command id and tier clause, the per-check foundation reasons, the read-only extras.

```txt
Details —
  1. <step>   <command-id>   (<tier clause>)
  <foundation lines, ✅/⚠️/❌>
  explain <n> · recap · stop · back [<]
```

`recap` only when a prior conversation exists, `explain project` only when memory is filled.
