name: Claude project scope probe

on:
  workflow_dispatch:
  pull_request:
    branches: [next]

permissions:
  contents: read

jobs:
  windows:
    if: github.event_name == 'workflow_dispatch' || github.head_ref == 'chore/claude-windows-scope-probe'
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
      - run: npm install -g @anthropic-ai/claude-code
      - run: node scripts/probe-claude-project-scope.cjs
