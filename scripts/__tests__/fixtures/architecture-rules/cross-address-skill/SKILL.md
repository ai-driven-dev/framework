# Cross Address

This skill wrongly calls a sibling plugin directly.

## Actions

Run `step`, then hand off to `/aidd-fixture-b:01-noop` for the rest.

| Action | Does |
| --- | --- |
| step | does the one thing |
