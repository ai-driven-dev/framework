# Bare Address

Declare it the same way `aidd-fixture-b:01-noop` does. Do not confuse this with
some-aidd-fixture-b:02-other, which names no real plugin and must stay silent.

## Actions

| Action | Does |
| --- | --- |
| `step` | does the one thing |
