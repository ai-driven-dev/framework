# Nested Under Assets

This file sits under an `assets/` segment even though it is named `SKILL.md` and would
otherwise be governed at this depth. Naming a sibling here, `aidd-fixture-b:01-noop`, must
stay silent.
