---
paths:
  - "src/**/*.ts"
  - "tests/**/*.ts"
---

# Biome Tooling

- Use biome for lint and format
- Run `pnpm lint` before committing
- Import order: node builtins first, then external, then internal
